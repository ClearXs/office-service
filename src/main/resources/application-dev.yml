spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/turbo_office
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: localhost
      port: 6379
      password: redis
      database: 1

logging:
  level:
    cc.allio.uno: debug
